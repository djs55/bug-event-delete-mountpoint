version: "3.7"

volumes:
  node_modules:
services:
  webapp:
    image: alpine
    command: tail -f /dev/null
    volumes:
      - COMPOSEDIR/webapp:/webapp:consistent
      - node_modules:/webapp/node_modules/
